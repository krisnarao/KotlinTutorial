import { useState } from "react";
import {
  Card,
  CardContent,
  Typography,
  Grid,
  TextField,
  MenuItem,
  Button,
  Alert,
} from "@mui/material";

const durations = [15, 30, 60, 120];

function RequestForm() {
  const [form, setForm] = useState({
    serverId: "",
    secretId: "",
    incidentId: "",
    duration: 30,
    justification: "",
    requestedBy: "admin",
  });

  // Temporary data (replace with API data later)
  const servers = [
    { id: 1, hostname: "APP-PROD-01" },
    { id: 2, hostname: "DB-PROD-01" },
    { id: 3, hostname: "API-PROD-01" },
  ];

  const secrets = [
    { id: 1, name: "DB_PASSWORD" },
    { id: 2, name: "JWT_KEY" },
    { id: 3, name: "TLS_CERTIFICATE" },
  ];

  const handleChange = (e) => {
    setForm({
      ...form,
      [e.target.name]: e.target.value,
    });
  };

  const submit = () => {
    console.log(form);
    // TODO: call BreakGlass API
  };

  return (
    <Card sx={{ borderRadius: 4 }}>
      <CardContent>

        <Typography variant="h5" fontWeight="bold" gutterBottom>
          Emergency Break Glass Request
        </Typography>

        <Alert severity="warning" sx={{ mb: 3 }}>
          Emergency access is fully audited and time limited.
        </Alert>

        <Grid container spacing={2}>

          <Grid item xs={12} md={6}>
            <TextField
              select
              fullWidth
              label="Server"
              name="serverId"
              value={form.serverId}
              onChange={handleChange}
            >
              {servers.map((server) => (
                <MenuItem
                  key={server.id}
                  value={server.id}
                >
                  {server.hostname}
                </MenuItem>
              ))}
            </TextField>
          </Grid>

          <Grid item xs={12} md={6}>
            <TextField
              select
              fullWidth
              label="Secret"
              name="secretId"
              value={form.secretId}
              onChange={handleChange}
            >
              {secrets.map((secret) => (
                <MenuItem
                  key={secret.id}
                  value={secret.id}
                >
                  {secret.name}
                </MenuItem>
              ))}
            </TextField>
          </Grid>

          <Grid item xs={12} md={6}>
            <TextField
              fullWidth
              label="Incident ID"
              name="incidentId"
              placeholder="INC-2026-10021"
              value={form.incidentId}
              onChange={handleChange}
            />
          </Grid>

          <Grid item xs={12} md={6}>
            <TextField
              select
              fullWidth
              label="Access Duration"
              name="duration"
              value={form.duration}
              onChange={handleChange}
            >
              {durations.map((d) => (
                <MenuItem key={d} value={d}>
                  {d} Minutes
                </MenuItem>
              ))}
            </TextField>
          </Grid>

          <Grid item xs={12}>
            <TextField
              fullWidth
              label="Business Justification"
              multiline
              rows={4}
              name="justification"
              value={form.justification}
              onChange={handleChange}
              placeholder="Explain why emergency access is required..."
            />
          </Grid>

          <Grid item xs={12}>
            <TextField
              fullWidth
              label="Requested By"
              value={form.requestedBy}
              InputProps={{
                readOnly: true,
              }}
            />
          </Grid>

          <Grid item xs={12}>
            <Button
              variant="contained"
              size="large"
              fullWidth
              onClick={submit}
              sx={{
                height: 50,
                fontWeight: "bold",
              }}
            >
              Submit Break Glass Request
            </Button>
          </Grid>

        </Grid>

      </CardContent>
    </Card>
  );
}

export default RequestForm;
